# 笔记写作详细准则

## 目标

当用户要求“总结整理一下所有对话”“做成笔记”“整理成笔记”“保存到 Obsidian/FlexNote/Rebirth”时，默认产出应该是可长期复习的学习笔记，而不是聊天摘要。

笔记必须详细、准确、讲清楚。目标是让没有参与原始对话的人，也能看懂问题背景、核心概念、推导过程、实操方法和常见误区。

除非用户明确要求“简单总结”“简短记录”“只要要点”，否则不要把笔记压缩成几句话。

## 总原则

- 不要只列结论，要解释结论从哪里来、为什么成立、适用于什么场景。
- 不要只复述最后一条回答，要尽量覆盖整轮对话中的关键问题、追问、纠错、例子和最终结论。
- 不要把聊天顺序机械复制成笔记。应该按学习顺序重组，让读者从基础概念逐步读到实战应用。
- 不要省略用户真正卡住的点。用户反复追问的地方，通常就是笔记最应该讲清楚的地方。
- 不要为了简洁牺牲准确性。技术名词、变量含义、空间归属、输入输出关系都要写明。

## 推荐结构

一篇技术学习笔记建议包含以下部分，可以按主题调整：

```text
1. 摘要
2. 问题背景
3. 核心概念
4. 基础原理
5. 计算公式或节点逻辑
6. 分步骤推导
7. 具体例子
8. 实战场景
9. 常见误区
10. 检查清单或分析模板
11. 重点结论
12. 待办或练习
```

不是每篇都必须机械包含所有标题，但必须保证读者能看懂：

```text
这个问题是什么
为什么会有这个问题
核心概念是什么
该怎么计算或操作
哪些地方容易错
以后遇到类似问题该如何分析
```

## 内容详略要求

### 1. 写清问题背景

笔记开头要交代用户在问什么、困惑点是什么。

例如：

```text
用户不是单纯想知道某个节点叫什么，而是在理解 UE 中世界空间、本地空间、屏幕空间、Niagara 粒子空间和材质空间之间如何互相转换。
```

这样后面讲概念和公式时，读者才知道为什么要学这些。

### 2. 先讲基础思维，再讲细节

不要一上来堆节点名或公式。应该先解释最底层的思维。

例如坐标转换类笔记，应先讲：

```text
坐标不是绝对数字，而是相对于某个空间的描述。
空间 = 原点 + X/Y/Z 三根轴 + 缩放。
Transform = 一个空间如何嵌进另一个空间。
坐标转换 = 把同一个点或方向换到另一个坐标系中重新描述。
```

然后再讲 `TransformPosition`、`WorldPositionOffset`、`ParticlePositionWS` 等具体节点。

### 3. 变量必须解释含义

涉及公式、代码或节点时，每个变量都要说明含义。

错误写法：

```text
NewP = C + (P - C) * S
```

更好的写法：

```text
P = 当前点的位置
C = 缩放中心
S = 缩放比例

NewP = C + (P - C) * S

含义是：先把当前点变成相对中心的偏移，再缩放这个偏移，最后加回中心位置。
```

### 4. 分清上下文

同一个概念在不同软件模块里可能不一样。笔记要写清楚上下文。

例如 UE 笔记中要区分：

```text
普通 Mesh Material
Post Process Material
Niagara System
Niagara Emitter
Niagara Particle
Niagara Renderer
Blueprint
C++
Custom HLSL
```

不要把材质阶段、粒子模拟阶段、屏幕后处理阶段混在一起讲。

### 5. 分清数据类型和空间

技术笔记里，坐标、向量、法线、UV、屏幕坐标等必须写清类型和空间。

例如：

```text
Position / Location：
    表示“在哪里”，会受平移影响。

Vector / Direction：
    表示“朝哪里”，不应该受平移影响。

Normal：
    是方向的一种，遇到非均匀缩放时要更小心。

UV：
    是面上的 2D 坐标，不等同于世界坐标。

Screen Position：
    是屏幕空间坐标，不等同于对象本地坐标。
```

建议变量名带空间后缀：

```text
Pos_WS = World Space Position
Pos_LS = Local Space Position
Center_WS = World Space Center
Center_LS = Local Space Center
Dir_WS = World Space Direction
```

### 6. 给出可代入的例子

抽象概念必须尽量配例子，尤其是用户正在学原理时。

例如讲空间转换时，不要只说：

```text
Local 转 World 就是乘 Transform。
```

应该给出数字：

```text
Location = (1000, 0, 0)
Yaw = 90°
LocalPos = (100, 20, 0)

本地 X 轴转到世界 Y 方向，本地 Y 轴转到世界 -X 方向。

WorldPos
= (1000, 0, 0)
+ (0, 1, 0) * 100
+ (-1, 0, 0) * 20
= (980, 100, 0)
```

这样读者能真正看到计算是怎么发生的。

### 7. 写清常见错误

笔记必须记录容易踩坑的地方。

例如 UE 材质坐标笔记中，要明确写：

```text
World Position Offset 输入的是世界空间偏移量，不是最终世界位置。

正确：
WPO = NewWorldPosition - OldWorldPosition

错误：
直接把 NewWorldPosition 接到 WPO
```

再比如：

```text
如果用 AbsoluteWorldPosition 直接计算方块中心，方块网格会钉在世界坐标上。
模型移动后，缩放中心会变化。

如果希望方块中心跟着模型走，应该先转到 Local/Object Space 里计算中心，再转回 World Space 输出 WPO。
```

### 8. 给出检查清单

长笔记末尾应给出可以复用的分析模板。

例如坐标计算类问题可以检查：

```text
1. 当前坐标在哪个空间？
2. 目标空间是什么？
3. 这是 Position 还是 Vector？
4. 这个中心到底是哪种中心？
5. 计算发生在材质、Niagara、蓝图、C++ 还是 Post Process？
6. Transform 是谁到谁？
7. 最终输出要的是位置、方向、颜色、偏移量还是屏幕坐标？
8. 如果跨系统传数据，双方是否使用同一个空间？
```

## UE / Niagara / Houdini / HLSL 笔记专项要求

针对 UE、Niagara、Houdini、HLSL、材质、节点图、坐标计算等技术主题，必须特别写清楚：

- 每个坐标、属性、参数属于什么空间或上下文。
- 每个输入从哪里来，输出接到哪里。
- Position、Vector、Normal、UV、Screen Position 等类型差异。
- 节点、代码、材质阶段、Niagara 阶段之间的边界。
- 哪些值是用户可调参数。
- 哪些计算用于避免首帧、零向量、空间错乱、漂移、边界错误或除零问题。
- 如果用了 Custom HLSL，要明确它是 Custom HLSL，不要伪装成普通节点。
- 如果是节点连接说明，要写清每个关键节点的输入 pin 和输出 pin。

## 语言与格式

- 默认使用中文写笔记。
- 技术名词保持常用英文写法，例如 `UE`、`Niagara`、`Houdini`、`HLSL`、`Blueprint`、`Material`、`Transform`、`World Position Offset`、`AbsoluteWorldPosition`。
- 英文技术名词左右保留空格，避免中英文挤在一起。
- 代码、公式、节点名、路径、属性名、参数名使用行内代码或代码块。
- 长笔记要用清楚的小标题组织，不要堆成长段落。
- 可以在末尾加“速查口诀”“分析模板”“重点结论”，方便以后复习。

## 最低完成标准

一篇合格的详细笔记至少要做到：

```text
读者不看原始对话，也能明白这个问题为什么重要。
读者能看懂核心概念，而不是只看到结论。
涉及计算时，有公式、变量解释和例子。
涉及软件操作时，有步骤、节点/属性来源和输出位置。
涉及容易混淆的概念时，有对比和误区说明。
文末有可复用的总结或检查清单。
```

如果只写几句话、只列关键词、只给最终答案，就不符合这个准则。
